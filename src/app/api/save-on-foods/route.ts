import { NextResponse } from "next/server";

export async function GET() {
  // const dom = JSDOM.fromURL("https://example.com/", options).then(dom => {
  //     console.log(dom.serialize());
  //   });
  return NextResponse.json({});
}
